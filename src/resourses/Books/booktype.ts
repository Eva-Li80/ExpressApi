// import Joi from "joi";

// export interface Book {
//   id: string;
//   title: string;
//   author: string;
// }

// export const bookSchema = Joi.object<Book, true>({
//   id: Joi.string().required().min(1),
//   title: Joi.string().required().min(1),
//   author: Joi.string().required().min(1),
// });
